extends layout.pug

block content
    section#content
        if name 
            h2 Welcome, #{name}!
            form(action='/', method='post')
                button(type='submit') Goodbye!
        else
            h2 Welcome, Student!
            form(action='/', method='post')
                button(type='submit') Login!